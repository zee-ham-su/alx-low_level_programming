#include <stdio.h>
#include "main.h"

/**
 * set_string - allocates the pointer value to char
 * @to: destination address
 * @s: data source
 * Return: none
 */

void set_string(char **s, char *to)
{
char **s;
*s = to;
}

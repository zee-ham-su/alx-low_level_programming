#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * malloc_checked - memory allocation using malloc
 * @b: bytes quantity
 * Return: allocated memory pointer
 */

void *malloc_checked(unsigned int b)
{

void *ptr = malloc(b);
return (ptr ? ptr : exit(98));

}

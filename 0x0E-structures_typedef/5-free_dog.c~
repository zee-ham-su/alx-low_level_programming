#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "dog.h"



/**
 * free_dog - frees dogs.
 * @d: struct dog.
 *
 * Return: no return.
 */



void free_dog(dog_t *d)
{

size_t i;
for (i = 0; i <= strlen(name); i++)

if (d == NULL)
{
printf("Error: the dog pointer is NULL.\n");
return;
}
free(d->owner);
free(d->name);
free(d);
}
